[build]
  command = "npm run build"
  publish = "dist"
  functions = "functions"

[functions]
  node_bundler = "esbuild"
	directory = ".netlify/functions"
	external_node_modules    = ["nodemailer"]

[[redirects]]
from = "/*"
to   = "https://www.rsdepannage.com/:splat"
status = 301
force  = true 